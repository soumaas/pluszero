問１（記述）
この問題は穴埋め問題です。次の（ア）から（ヌ）を埋めて下さい。

(1) グラフィカルなデザインを用いたパソコン操作画面を(GUI)と呼ぶ。直感的にわかりやすく、人間がコンピュータを操作する上で非常に便利である。画像やアイコンが無く、無い文字だけでコンピュータを操作する画面を(CUI)と呼び、操作性が非常にシンプルなのが特徴である。

(2) (Linux)とは、世界で最も普及している、オープンソースのオペレーティングシステムであり、基本的な操作は(CUI)で行うため、プログラミング言語を動かす環境としてとても相性が良い。

(3) (CUI)での操作を考える。ターミナル上での操作で、/user/test/というディレクトリに移動し、document.txtというファイルを作成する場合、

~$ cd /user/work/test/
/user/work/test ~$（touch）document.txt

とする。また、workディレクトリと同じ階層にprivateというディレクトリを作成し、testディレクトリをコピーし、privateディレクトリの中身を表示するには、

/user/work/test ~$ cd
/user ~$ mkdir private
/user ~$ cd private
/user/private ~$ cp test
/user/private ~$ ls

とする。　
ルートユーザ権限が必要な場合は、（sudo）をつけることで操作が可能になる。
その他、ファイル削除には（rm）コマンド、（ファイルを移動）するにはmvコマンド、カレントディレクトリの確認には（pwd）を使用する。
また、コマンドの履歴は（history）で表示できるので、過去に使用したコマンドを使用したい場合に便利である。

(4) pythonのパッケージ管理ツールpipの代表的なコマンドには、（インストールされたパッケージを確認）できるfreeze（特定のパッケージの場合には（show）を使う）、バージョンを確認できる（-V）、パッケージを一覧で確認できる（list）などがある。

(5) ターミナル上での作業を効率的に行うには、tmuxを使うとよい。
利点としては、（前回の作業から、再開できること）や（1つのターミナルで複数のターミナルを実行できること）がある。

(6) (github)とは、分散型バージョン管理システムの一つで、もともとLinuxのソースコードを効果的に管理するために開発された。(リポジトリ)とは、ファイルやディレクトリの状態を記録する場所で、専用のサーバに配置して複数人で共有する(リモートリポジトリ)とユーザ一人ひとりが利用するために、自分の手元のマシン上に配置する(ローカルリポジトリ)の2種類ある。

(7) コンピュータの部品の中で、連続的な計算処理が得意なのは(CPU)である。また、並列的な計算処理が得意なのは(GPU)であり、元々膨大な計算処理を必要とする3Dグラフィックス用途に設計されたが、高い演算性能から(画像処理)に使われている。
